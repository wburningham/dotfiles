# Initialize the completion system
autoload -Uz compinit && compinit

# Trying out this alias
alias c='chezmoi'

# Source ENV variables
[ -f "$HOME/.env" ] && . "$HOME/.env"


# use mcfly for ctrl+r
eval "$(mcfly init zsh)"
